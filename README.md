# mobile_fitness_app

## Requirements
- Flutter SDK installed and on PATH
- Platform toolchain (Android Studio / Xcode) set up

## Env
- Copy `.env.example` to `.env`
- Set `BACKEND_URL` (API base URL)

## Run
```bash
flutter run --release
```

## Build
```bash
flutter build apk --release
flutter build ios --release
```
